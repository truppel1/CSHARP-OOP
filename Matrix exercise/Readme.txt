Make a program to read two integers M and N, and then read
a matrix of M rows by N columns containing integers,
there may be.

Then read an integer X that belongs to the matrix.
For each occurrence of X, show the values ​​at
left, above, right and below X, if any.